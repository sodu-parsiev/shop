[www]
; Tuned for a small VPS (2 vCPU / ~2GB RAM) — avoid spawning more workers
; than the box can hold in memory alongside MySQL and Nginx.
pm = dynamic
pm.max_children = 6
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
pm.max_requests = 500
