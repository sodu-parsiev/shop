document.addEventListener('alpine:init', () => {
    // Register Alpine.data() components here as the public frontend grows.
    // Alpine itself is provided by Livewire's bundled build (see @livewireScripts).
});
